baseURL = "https://sivaprasadreddy.github.io/sivalabs-dev/"
languageCode = "en-us"
title = "SivaLabs"
uglyURLs = true
disableKinds = ["taxonomy", "term"]

[caches]
  [caches.images]
    dir = ':cacheDir/images'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
